# .env.example
# ------------------------------------------------------------------------------
# This file is a template for environment variables required by the project.
# Copy this file to `.env` and replace the placeholder values with your actual
# credentials and configuration settings. NEVER commit the real `.env` file.
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# API KEYS
# ------------------------------------------------------------------------------
# API key for accessing the GovInfo API. This is required for fetching bill data.
GOVINFO_API_KEY=REPLACE_WITH_YOUR_GOVINFO_API_KEY

